import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Menu = () => {
  return (
    <View>
      <Text>Menu</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Menu;
